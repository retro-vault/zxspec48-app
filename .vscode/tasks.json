{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build ZX Spectrum app",
      "type": "shell",
      "command": "make",
      "args": ["TARGET=zxspec48-app"],
      "options": { "cwd": "${workspaceFolder}" },
      "group": { "kind": "build", "isDefault": true },
      "problemMatcher": ["$gcc"]
    }
  ]
}
